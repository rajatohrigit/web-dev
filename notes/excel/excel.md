# Excel



* ctrl+ arrow key to come to move
* ctr + plus - Insert a row/column(by default it insert row)
* ctr + minus - Delete a row/column(by default it delete column)

&#x20;  or we can click on insert and delete option in home tab as well

* To use this commands please select the specific row or column you can do this easily with (ctrl + shift + arrow key)





##### ctrl +T - Dynamic table or in Home section choose format as table

1. For deleting dynamic table/convert back to original form 
2. Go to table design 
3. Convert to range then after selecting table 
4. Go to home section 
5. Select clear format



* **To do data entry fast go to quick access toolbar, then more commands and add form in excel then we can do it faster, with tab and enter**





* ##### To convert horizontal to vertical format(but not dynamic table) and vice versa

  1. copy the data with ctrl+ C then select area where you want to insert then right click paste special choose transpose 
  2. or use shortcut for paste special (ctrl + alt + V)





* **To apply filter to access certain data we can go to home section then choose sort \& filter then filter and choose convienently by clicking on arrow button on the column**

&#x20;  **(in dynamic table filters can easily used without manually command it we can see on every column arrow button)**



* **In dynamic tables, in the table design section there is a option called total rows we can check it and use that features**



* **To remove duplicates, go to data and choose remove duplicates**





* ##### To make a column, a drop down list 

  1. Go to data then data validation 
  2. In the settings in allow section 
  3. Choose list then in the source mention your list items separated by comma or define the source by selecting cell address by dragging if data is present already
  4. We can clear drop down list in the data validation then setting press clear all and on the find \& select section there is a data validation also where you check 

&#x20;   where data validation existed it will automatically select





* **To remove blank rows, Ctrl+ G (for Goto) or (in home section select find \& select then press Goto)**

&#x20;  **then press special tick the blanks then ok it will select all the blank rows then do ctrl+ minus** 



* **If we want to copy the excel table from google, when we extract text and paste it on the excel its format will be different to maintain that format we have** 

&#x20;  **to copy the unformatted table anywhere in excel and we have to assume 2 alphabets and with the specified place according to the place value for ex. abA1 abA2** 

&#x20;  **we have to mention it manually for 2 rows and we simply drag it according to the total rows and we have to click on home section, find \& select and find replace**

&#x20;  **option or press command directly \[ctrl + H] and then select the table where assumed alphabets are selected then on find what section enter the alphabets and** 

&#x20;  **replace with = and copy the complete table output then right click the mouse then paste special\[Ctrl +Alt+ V]** 

&#x20;  **then set as values then paste as values then we can easily delete the unformatted table because the reference is longer needed** 



* **If data is present like this with a delimiter, when data is looking like in row format for ex name , class , age , course** 

&#x20;  **then go to data-> text to column->delimited->next->choose delimiter like space , tab , semicolon , commas then finish** 



* **To copy the entire sheet just press control and drag it to  and move to new sheet**



* **autofit column -> alt + H + O + I**
* **autofit row-> alt + H + O + A**



* **To combine text from multiple columns, use ctrl + E (flash fill) for ex first name with last name in full name**



* **=trim(cell address) remove extra spaces from the text use this formula after creating new column ,then copy the column \& paste special**

&#x20;  **(ctrl + alt + V ) as select values then we can delete the unformatted column.** 



* **If any date is given we can find any day,by using formula =text(cell\_address,"dddd") it will automatically fetch the current day of the following date**



* **To select all the empty blanks,ctrl+G(goto) then special-> blanks then it will automatically select blank rows then if we want to name them by NA then press ctrl+ enter then all the blank rows will be represented as NA**



* **To remove gridlines \[go to page layout then unselect the gridline view]**



* **To freeze the header row \[go to view-> freeze panes->freeze top row]**



* **To convert in upper case upper(celladdress)**

&#x20;  **then paste as values because it will take reference**



* **When we have big amount of data we want to find any particular data in seconds we can use**

&#x20;  **=vlookup(search\_key,table,searching\_column\_number,0) fix it with f4(with this result will come accurate)**

&#x20;  **=xlookup(search\_key,search\_key\_range,result\_range) we can done it horizontally as well**





# 

